# ============================================================================
# pip.ini - Python pip 包管理器配置（Windows 上的 AppData 位置）
# ============================================================================
# 说明: 这是 Python 的 pip 包管理器需要的配置文件
# 用途: 配置 pip 的软件下载渠道，使用国内镜像以加快下载
# 适用: Windows 上使用 Python 的用户
# 执行时机: pip 作业时使用（安装或更新包）
# 共用配置: 内容与 ~/.pip/pip.conf (Linux/macOS) 一致，由 .chezmoitemplates/pip-config 统一管理
# ============================================================================

{{ template "pip-config" . }}
